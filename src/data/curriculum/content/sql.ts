import type { TrackContent } from '../enrich'

export const sqlContent: TrackContent = {
  topics: {
    'SQL Fundamentals': [
      ['What is SQL?', 'An absolute beginner\'s introduction to SQL, explaining what it is and why it matters.', ["Core concept of SQL","Mental models","Basic terminology"], [["What exactly is SQL?","It is a fundamental concept/tool in modern software engineering."],["Why should I learn this?","It provides the foundation for building scalable and reliable systems."],["Is it difficult for beginners?","Like anything, it takes practice, but the core concepts are very approachable."]]],
      ['Why use SQL?', 'Understanding the real-world applications and use cases of SQL in modern engineering.', ["Industry use cases","Alternatives","When NOT to use it"], [["Where is SQL used?","In almost every modern tech stack and enterprise system."],["Are there alternatives?","Yes, but this remains a highly requested skill."],["Does every project need this?","Not necessarily, it depends on the scale and requirements of the project."]]],
      ['History and Ecosystem', 'A brief look at how SQL evolved and where it fits in the broader ecosystem today.', ["Origins and evolution","Current ecosystem","Future trends"], [["How has it evolved?","It has become more standardized, efficient, and easier to use over time."],["What is the ecosystem like?","Very rich, with massive community support and tooling."],["Is it still relevant today?","Yes, it is more relevant than ever."]]],

      ['Relational database fundamentals', 'Tables of rows and columns related by keys. The relational model, declarative queries and why databases beat files.', ['Relations, rows, columns', 'Declarative querying', 'RDBMS responsibilities'], [['What is a relation?', 'A table: a set of tuples with the same attributes.'], ['Why is SQL declarative?', 'You say what you want; the planner decides how.'], ['Name three things an RDBMS guarantees.', 'Durability, concurrency control, integrity constraints.']]],
      ['Tables', 'CREATE TABLE with columns, defaults, NOT NULL and identity columns; ALTER and DROP with care.', ['CREATE TABLE', 'Defaults and identity', 'ALTER TABLE'], [['How do you auto-generate ids in Postgres?', 'GENERATED ALWAYS AS IDENTITY (or serial).'], ['Does ALTER TABLE lock the table?', 'Often; plan schema changes for low traffic.'], ['What does DROP TABLE do to dependent views?', 'Fails unless CASCADE is used.']]],
      ['Schemas', 'Namespaces inside a database for organising tables and permissions. search_path and qualified names.', ['Schemas as namespaces', 'search_path', 'Permissions per schema'], [['Default schema in Postgres?', 'public.'], ['Why use multiple schemas?', 'Separation by domain or tenant and cleaner permissions.'], ['What is a fully qualified name?', 'schema.table.']]],
      ['Types', 'Numeric, text, boolean, date/time, JSON, arrays and UUID. Choosing types affects storage, correctness and indexing.', ['Numeric and text types', 'Date, time, timestamptz', 'JSON, arrays, UUID'], [['Why prefer timestamptz?', 'It stores an absolute instant and converts per session zone.'], ['NUMERIC versus FLOAT for money?', 'NUMERIC; floats are inexact.'], ['Difference between text and varchar(n)?', 'Only the length limit; performance is the same in Postgres.']]],
      ['Keys', 'Primary keys identify rows; foreign keys enforce relationships; natural versus surrogate keys.', ['Primary keys', 'Foreign keys', 'Natural versus surrogate'], [['Can a primary key be null?', 'No.'], ['What does ON DELETE CASCADE do?', 'Deletes child rows when the parent is deleted.'], ['UUID or bigint for ids?', 'bigint is smaller and faster; UUID avoids coordination and guessing.']]],
      ['Constraints', 'NOT NULL, UNIQUE, CHECK, FOREIGN KEY and exclusion constraints keep bad data out at the source.', ['Column and table constraints', 'CHECK expressions', 'Deferrable constraints'], [['Can UNIQUE columns contain multiple NULLs?', 'Yes in Postgres, unless NULLS NOT DISTINCT.'], ['Why put constraints in the database, not only the app?', 'Every writer is covered, including scripts and bugs.'], ['What is a deferrable constraint?', 'One checked at commit instead of per statement.']]],
    ],
    'CRUD Operations': [
      ['SELECT', 'Projection, aliases, expressions, DISTINCT, LIMIT/OFFSET and ordering. The starting point of every query.', ['Projection and aliases', 'DISTINCT and LIMIT', 'Expressions and functions'], [['What does SELECT DISTINCT do?', 'Removes duplicate rows from the result.'], ['Is SELECT * bad?', 'In application code yes: fragile and fetches more than needed.'], ['Evaluation order of a SELECT?', 'FROM, WHERE, GROUP BY, HAVING, SELECT, ORDER BY, LIMIT.']]],
      ['INSERT', 'Single and multi-row inserts, RETURNING, defaults and upsert with ON CONFLICT.', ['Multi-row INSERT', 'RETURNING', 'ON CONFLICT upsert'], [['How do you get the generated id back?', 'INSERT ... RETURNING id.'], ['What does ON CONFLICT DO NOTHING do?', 'Skips rows that violate a unique constraint.'], ['Why batch inserts?', 'Fewer round trips and faster loads.']]],
      ['UPDATE', 'Set columns with expressions, update from joins, and always write the WHERE clause first.', ['UPDATE ... SET', 'UPDATE with FROM/JOIN', 'Safety practices'], [['What happens with UPDATE without WHERE?', 'Every row changes.'], ['How do you update using another table?', 'UPDATE t SET ... FROM other WHERE ...'], ['Can UPDATE return rows?', 'Yes, with RETURNING.']]],
      ['DELETE', 'Remove rows by condition, delete using joins, and TRUNCATE for whole tables. Soft deletes as an alternative.', ['DELETE with conditions', 'TRUNCATE', 'Soft deletes'], [['Difference between DELETE and TRUNCATE?', 'TRUNCATE is faster, resets identity, and cannot filter rows.'], ['What is a soft delete?', 'A deleted_at column instead of removing the row.'], ['Delete rows matching another table?', 'DELETE FROM t USING other WHERE ...']]],
      ['WHERE', 'Filtering with comparisons, BETWEEN, IN, LIKE, IS NULL and boolean logic. NULL comparisons need special care.', ['Comparison operators', 'NULL semantics', 'Pattern matching'], [['Why does col = NULL never match?', 'NULL is unknown; use IS NULL.'], ['Difference between LIKE and ILIKE?', 'ILIKE is case-insensitive (Postgres).'], ['Is BETWEEN inclusive?', 'Yes, both ends.']]],
      ['ORDER BY', 'Sort by columns or expressions, ascending or descending, with NULLS FIRST/LAST, and why unsorted results are unordered.', ['Sorting basics', 'NULL ordering', 'Sorting cost'], [['Is result order guaranteed without ORDER BY?', 'No.'], ['Where do NULLs sort by default in Postgres ascending?', 'Last.'], ['Can ORDER BY use a column not in SELECT?', 'Yes, unless DISTINCT is used.']]],
      ['GROUP BY', 'Aggregate rows into groups with COUNT, SUM, AVG, MIN, MAX. Every non-aggregated column must be grouped.', ['Aggregate functions', 'Grouping rules', 'Grouping sets and rollup'], [['What does COUNT(*) versus COUNT(col) count?', 'All rows versus non-null values of col.'], ['Why does selecting an ungrouped column fail?', 'The database cannot pick which row\'s value to show.'], ['What is ROLLUP?', 'Adds subtotal rows for hierarchical grouping.']]],
      ['HAVING', 'Filter groups after aggregation, unlike WHERE which filters rows before it.', ['WHERE versus HAVING', 'Filtering on aggregates', 'Combining both'], [['Can WHERE use COUNT(*)?', 'No, aggregates need HAVING.'], ['Which runs first, WHERE or HAVING?', 'WHERE.'], ['Find customers with more than 5 orders?', 'GROUP BY customer HAVING COUNT(*) > 5.']]],
    ],
    'Joins and Advanced Queries': [
      ['INNER JOIN', 'Rows with matching keys in both tables. The default join; understand join conditions versus filters.', ['Join syntax', 'ON versus WHERE', 'Multi-table joins'], [['What does an inner join drop?', 'Rows without a match on either side.'], ['Result of joining on a non-unique key?', 'Row multiplication.'], ['Is JOIN the same as INNER JOIN?', 'Yes.']]],
      ['LEFT JOIN', 'All rows from the left table with NULLs where the right has no match. Used for "customers without orders" queries.', ['Preserving left rows', 'Filtering after a left join', 'Anti-join pattern'], [['Why does a WHERE on the right table turn a LEFT JOIN into inner?', 'NULL rows fail the filter; put the condition in ON.'], ['Find rows with no match?', 'LEFT JOIN ... WHERE right.id IS NULL.'], ['Does LEFT JOIN change the row count of the left table?', 'It can increase it with multiple matches.']]],
      ['RIGHT JOIN', 'Mirror of LEFT JOIN. Rarely needed; rewrite as LEFT JOIN by swapping tables for readability.', ['Semantics', 'Rewriting as LEFT JOIN', 'When it appears'], [['Equivalent of A RIGHT JOIN B?', 'B LEFT JOIN A.'], ['Why is RIGHT JOIN uncommon?', 'Readers expect the preserved table first.'], ['Which rows get NULLs?', 'Left-table columns for unmatched right rows.']]],
      ['FULL JOIN', 'All rows from both sides, NULL-filled where unmatched. Useful for reconciliations and diffs.', ['Semantics', 'Reconciliation queries', 'COALESCE on keys'], [['How do you list mismatches on either side?', 'FULL JOIN ... WHERE a.id IS NULL OR b.id IS NULL.'], ['Why COALESCE(a.id, b.id)?', 'To get a single key column from either side.'], ['Does MySQL support FULL JOIN?', 'No, emulate with UNION of LEFT and RIGHT joins.']]],
      ['Subqueries', 'Queries inside queries: scalar, IN/EXISTS, correlated subqueries and derived tables in FROM.', ['Scalar and IN subqueries', 'EXISTS and correlated subqueries', 'Derived tables'], [['Difference between IN and EXISTS?', 'EXISTS stops at the first match and handles NULLs safely.'], ['What is a correlated subquery?', 'One that references the outer row and runs per row.'], ['Why avoid NOT IN with NULLs?', 'A NULL in the list makes NOT IN return no rows.']]],
      ['CTEs', 'WITH clauses name intermediate results for readability, and recursive CTEs walk hierarchies.', ['WITH syntax', 'Recursive CTEs', 'Materialisation'], [['How do you traverse a tree of managers?', 'WITH RECURSIVE anchored at the root and joined to itself.'], ['Are CTEs always materialised in Postgres?', 'Not since 12; inline unless MATERIALIZED.'], ['Can a CTE be referenced twice?', 'Yes.']]],
      ['Window functions', 'Aggregates over a window of rows without collapsing them: ROW_NUMBER, RANK, LAG, LEAD, running totals.', ['OVER, PARTITION BY, ORDER BY', 'Ranking functions', 'LAG, LEAD and frames'], [['Top 1 per group?', 'ROW_NUMBER() OVER (PARTITION BY g ORDER BY x DESC) = 1.'], ['RANK versus DENSE_RANK?', 'RANK skips numbers after ties; DENSE_RANK does not.'], ['Running total?', 'SUM(x) OVER (ORDER BY date).']]],
    ],
    'Database Architecture': [
      ['Schema design', 'Model entities and relationships, pick keys, avoid nullable sprawl, and design for the queries you will run.', ['Entities and relationships', 'Choosing keys', 'Designing for queries'], [['One-to-many is modelled how?', 'A foreign key on the many side.'], ['Many-to-many?', 'A junction table with two foreign keys.'], ['What is an ERD?', 'A diagram of entities and their relationships.']]],
      ['Relationships', 'One-to-one, one-to-many, many-to-many and self-references, with referential integrity and cascade behaviour.', ['Cardinalities', 'Junction tables', 'Self-referencing tables'], [['Where does the foreign key go in one-to-one?', 'Either side, with a unique constraint.'], ['Example of a self-reference?', 'employees.manager_id → employees.id.'], ['What does ON DELETE SET NULL do?', 'Nulls the child reference when the parent is deleted.']]],
      ['Normalization', '1NF through 3NF (and BCNF) remove redundancy and update anomalies. Denormalise deliberately for read performance.', ['1NF, 2NF, 3NF', 'Anomalies', 'Denormalisation trade-offs'], [['What does 3NF forbid?', 'Non-key columns depending on other non-key columns.'], ['What is an update anomaly?', 'Changing a fact in one place but not another copy.'], ['When denormalise?', 'Read-heavy paths where joins are too costly, with a plan to keep copies in sync.']]],
      ['Indexes', 'B-tree indexes speed lookups and sorts at the cost of writes and space. Composite indexes, covering indexes and selectivity.', ['B-tree basics', 'Composite and covering indexes', 'Cost of indexes'], [['Column order in a composite index matters why?', 'The index supports prefixes: (a, b) helps a and a+b, not b alone.'], ['What is a covering index?', 'One containing all columns a query needs, avoiding table lookups.'], ['Why not index everything?', 'Every write updates every index.']]],
      ['Query execution plans', 'EXPLAIN ANALYZE shows scans, joins, estimated versus actual rows and where time goes.', ['Reading EXPLAIN', 'Scan and join types', 'Estimates versus actuals'], [['Seq scan versus index scan?', 'Full table read versus index lookup.'], ['What does a big gap between estimated and actual rows mean?', 'Stale statistics; run ANALYZE.'], ['What is a nested loop join good for?', 'Small outer sets with indexed inner lookups.']]],
    ],
    Transactions: [
      ['Transactions', 'BEGIN, COMMIT, ROLLBACK and savepoints. All-or-nothing groups of statements.', ['BEGIN, COMMIT, ROLLBACK', 'Savepoints', 'Autocommit'], [['What is a savepoint?', 'A point inside a transaction you can roll back to.'], ['Is each statement a transaction by default?', 'Yes, in autocommit mode.'], ['What happens on an error mid-transaction in Postgres?', 'The transaction is aborted until rollback.']]],
      ['ACID', 'Atomicity, consistency, isolation, durability: the guarantees that make transactions trustworthy.', ['The four properties', 'How databases implement them', 'Trade-offs'], [['What does durability rely on?', 'Write-ahead logging flushed to disk.'], ['Which property do isolation levels weaken?', 'Isolation.'], ['What is consistency in ACID?', 'Constraints hold before and after each transaction.']]],
      ['Isolation levels', 'Read uncommitted, read committed, repeatable read, serializable, and the anomalies each allows.', ['The four levels', 'Anomalies: dirty, non-repeatable, phantom', 'Postgres default'], [['Postgres default level?', 'Read committed.'], ['What is a phantom read?', 'A re-run query sees new rows inserted by another transaction.'], ['What does serializable guarantee?', 'Results equal some serial ordering of transactions.']]],
      ['Locking', 'Row and table locks, SELECT FOR UPDATE, lock queues and how MVCC lets readers avoid blocking writers.', ['Row versus table locks', 'FOR UPDATE and SKIP LOCKED', 'MVCC'], [['What does SELECT ... FOR UPDATE do?', 'Locks the selected rows until commit.'], ['Do readers block writers in Postgres?', 'No, thanks to MVCC.'], ['Use of SKIP LOCKED?', 'Job queues: workers take unlocked rows.']]],
      ['Deadlocks', 'Two transactions waiting on each other. Databases detect and abort one; prevent by consistent lock ordering.', ['How deadlocks arise', 'Detection and victims', 'Prevention'], [['How does Postgres handle a deadlock?', 'Detects it and aborts one transaction.'], ['Simplest prevention?', 'Acquire locks in the same order everywhere.'], ['Should the application retry after a deadlock?', 'Yes, the aborted transaction.']]],
      ['Concurrency', 'Lost updates, optimistic versus pessimistic control, version columns and idempotent writes.', ['Lost update problem', 'Optimistic locking', 'Pessimistic locking'], [['What is optimistic locking?', 'Update only if the version matches, else retry.'], ['When prefer pessimistic locking?', 'High contention where retries would be wasteful.'], ['What is a lost update?', 'Two writers overwrite each other\'s change.']]],
    ],
    'Advanced Features': [
      ['Views', 'Saved queries acting as virtual tables. Simplify access, enforce security, and materialised views cache results.', ['Creating views', 'Updatable views', 'Materialised views'], [['Does a view store data?', 'No, unless materialised.'], ['How do you refresh a materialised view?', 'REFRESH MATERIALIZED VIEW (CONCURRENTLY with a unique index).'], ['Security use of views?', 'Expose only certain columns or rows.']]],
      ['Triggers', 'Functions that fire on INSERT, UPDATE or DELETE. Useful for audit logs; dangerous when they hide logic.', ['BEFORE and AFTER triggers', 'Trigger functions', 'Pitfalls'], [['Common trigger use case?', 'Setting updated_at or writing audit rows.'], ['Why can triggers surprise developers?', 'Side effects happen invisibly.'], ['Row-level versus statement-level?', 'Fires per row versus once per statement.']]],
      ['Stored procedures', 'Server-side routines in PL/pgSQL or SQL. Encapsulate logic near data; weigh against application-side code.', ['Functions versus procedures', 'PL/pgSQL basics', 'When to use them'], [['Difference between function and procedure in Postgres?', 'Procedures can manage transactions and are called with CALL.'], ['Advantage of server-side logic?', 'Fewer round trips and consistent enforcement.'], ['Drawback?', 'Harder to version, test and debug.']]],
      ['Migrations', 'Versioned schema changes applied in order by tools (Drizzle, Flyway, Prisma). Forward-only, reviewed, and safe for live data.', ['Migration tools', 'Safe change patterns', 'Rollbacks'], [['How do you add a NOT NULL column safely?', 'Add nullable with a default, backfill, then set NOT NULL.'], ['Why avoid down migrations in production?', 'They rarely restore data; prefer forward fixes.'], ['What tracks applied migrations?', 'A migrations table in the database.']]],
      ['Schema evolution', 'Changing schemas without downtime: expand-contract, dual writes, and backward-compatible steps.', ['Expand and contract', 'Backfills', 'Coordinating with deploys'], [['What is expand-contract?', 'Add the new structure, migrate, switch code, then remove the old.'], ['Why avoid renaming a column in one step?', 'Old code breaks mid-deploy.'], ['How do you backfill large tables?', 'In batches to limit locks.']]],
    ],
    'Specific Databases': [
      ['PostgreSQL-specific features', 'JSONB, arrays, full-text search, CTE power, extensions (PostGIS, pg_trgm), and rich index types.', ['JSONB and GIN indexes', 'Arrays and extensions', 'Index types (GIN, GiST, BRIN)'], [['How do you index a JSONB column?', 'A GIN index.'], ['What does pg_trgm enable?', 'Fast LIKE and similarity searches.'], ['When use a BRIN index?', 'Huge tables ordered by insertion, like time series.']]],
      ['MySQL-specific features', 'InnoDB engine, clustered primary keys, auto_increment, and the syntax and behaviour differences from Postgres.', ['InnoDB and clustered indexes', 'Syntax differences', 'Strict mode'], [['Why does primary key choice matter more in InnoDB?', 'Rows are stored in primary key order (clustered).'], ['Equivalent of RETURNING in MySQL?', 'LAST_INSERT_ID() or separate SELECT.'], ['What does sql_mode STRICT do?', 'Rejects invalid data instead of silently adjusting it.']]],
      ['Partitioning', 'Split a large table into partitions by range, list or hash to speed queries and maintenance.', ['Partition strategies', 'Partition pruning', 'Maintenance benefits'], [['What is partition pruning?', 'The planner skipping partitions that cannot match.'], ['Common partitioning key?', 'Time, for logs and events.'], ['How do you drop old data cheaply?', 'DROP the partition.']]],
      ['Replication', 'Streaming replication for standbys, logical replication for selective sync, and replication lag.', ['Physical versus logical', 'Read replicas', 'Lag and consistency'], [['What is replication lag?', 'Delay before a replica reflects primary writes.'], ['Can you write to a streaming replica?', 'No, it is read-only.'], ['Use of logical replication?', 'Replicating selected tables or across versions.']]],
      ['High availability', 'Failover, connection routing, backups and testing recovery. Managed services automate most of it.', ['Failover', 'Health checks and routing', 'Testing recovery'], [['What is failover?', 'Promoting a replica when the primary fails.'], ['What is an RPO?', 'How much data loss is acceptable.'], ['Why test restores?', 'A backup is only real once restored.']]],
    ],
    Administration: [
      ['Backup', 'Logical dumps (pg_dump) versus physical base backups with WAL archiving for point-in-time recovery.', ['pg_dump', 'Physical backups and WAL', 'Schedules and retention'], [['What is point-in-time recovery?', 'Restoring to any moment using base backup plus WAL.'], ['Is pg_dump consistent?', 'Yes, it runs in a single transaction snapshot.'], ['Where should backups live?', 'Off the database host, ideally another region.']]],
      ['Recovery', 'Restoring dumps, replaying WAL, and rehearsing disaster scenarios with documented runbooks.', ['Restore procedures', 'Runbooks', 'Recovery drills'], [['What is an RTO?', 'The target time to restore service.'], ['First step in a data-loss incident?', 'Stop further writes and preserve current state.'], ['Why write a runbook?', 'Recovery under pressure must be repeatable.']]],
      ['Pooling', 'Connection poolers (PgBouncer) and application pools limit connections and protect the database.', ['Why pooling', 'PgBouncer modes', 'Sizing pools'], [['Why are Postgres connections expensive?', 'Each is a process with memory overhead.'], ['Transaction versus session pooling?', 'Transaction mode reuses connections per transaction but breaks session state.'], ['Rule of thumb for pool size?', 'Roughly cores × 2 plus disk spindles, then measure.']]],
      ['Security', 'Roles and grants, least privilege, row-level security, TLS and auditing.', ['Roles and privileges', 'Row-level security', 'Network security'], [['What is row-level security?', 'Policies filtering rows per user.'], ['Should the app connect as a superuser?', 'Never.'], ['How do you encrypt in transit?', 'Require TLS (sslmode=require or verify-full).']]],
      ['Query optimization', 'Find slow queries with pg_stat_statements, fix with indexes, rewrites, and avoiding N+1 patterns.', ['Finding slow queries', 'Fixing with indexes and rewrites', 'N+1 in applications'], [['What is the N+1 problem?', 'One query per row of a parent result instead of a join or IN.'], ['What does pg_stat_statements show?', 'Aggregate timing per query pattern.'], ['Why can a function on an indexed column prevent index use?', 'The index is on the raw value, not the expression.']]],
      ['Practical database projects', 'Design and build a schema for a real domain, seed it, write reports with window functions and tune with EXPLAIN.', ['Domain modelling', 'Seeding and reports', 'Tuning'], [['What makes a good practice domain?', 'Orders, users, products: enough relationships to be realistic.'], ['What reports exercise SQL well?', 'Top-N per group, retention cohorts, running totals.'], ['How do you prove tuning worked?', 'Before and after EXPLAIN ANALYZE numbers.']]],
    ],
  },
  add: {
    'CRUD Operations': [
      ['Aggregate and scalar functions', 'COUNT, SUM, AVG with FILTER, string functions, date arithmetic and COALESCE/NULLIF for null handling.', ['Aggregates with FILTER', 'String and date functions', 'NULL helpers'], [['What does COALESCE return?', 'The first non-null argument.'], ['Count only paid orders in one query?', 'COUNT(*) FILTER (WHERE status = \'paid\').'], ['Truncate a timestamp to the month?', 'date_trunc(\'month\', ts).']]],
      ['Set operations', 'UNION, UNION ALL, INTERSECT and EXCEPT combine result sets. UNION removes duplicates and costs more.', ['UNION versus UNION ALL', 'INTERSECT and EXCEPT', 'Column compatibility'], [['Why prefer UNION ALL when duplicates are impossible?', 'It skips the expensive dedup step.'], ['What does EXCEPT do?', 'Rows in the first query not in the second.'], ['Requirement for set operations?', 'Same number and compatible types of columns.']]],
    ],
    'Joins and Advanced Queries': [
      ['EXISTS, IN and anti-joins', 'Semi-joins and anti-joins express "has any" and "has none". Know which forms are NULL-safe and index-friendly.', ['Semi-join with EXISTS', 'Anti-join patterns', 'NULL pitfalls'], [['Safest anti-join?', 'NOT EXISTS.'], ['Why is NOT IN dangerous?', 'A NULL in the subquery yields no rows.'], ['Does EXISTS return columns from the subquery?', 'No, only true/false per outer row.']]],
      ['JSON in SQL', 'Store and query semi-structured data: JSONB operators, indexing, extracting fields and when to normalise instead.', ['JSONB operators', 'Indexing JSON', 'When not to use JSON'], [['Extract a text field from JSONB?', 'data->>\'name\'.'], ['Check containment?', 'data @> \'{"a": 1}\'.'], ['When avoid JSON columns?', 'Fields you filter, join or constrain often.']]],
    ],
    'Database Architecture': [
      ['Full-text search', 'tsvector and tsquery, GIN indexes, ranking, and when to move to a dedicated search engine.', ['tsvector and tsquery', 'GIN indexes and ranking', 'Postgres versus Elasticsearch'], [['How do you rank results?', 'ts_rank on the tsvector and query.'], ['What does to_tsvector do?', 'Normalises text into lexemes.'], ['When is Postgres FTS enough?', 'Moderate data and simple relevance needs.']]],
    ],
  },
}
